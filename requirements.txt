# Core dependencies
pyyaml>=6.0.1

# Web crawling and content extraction
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Network analysis (for graph visualization and PageRank)
networkx>=3.0

# EXPERT-LEVEL FEATURES
# =====================

# Text embeddings for semantic similarity (THE KEY to finding similar pages)
sentence-transformers>=2.2.0
torch>=2.0.0  # Required by sentence-transformers

# Named Entity Recognition (THE KEY to understanding what pages are ABOUT)
spacy>=3.5.0
# After install, download model: python -m spacy download en_core_web_sm

# Topic modeling (THE KEY to automatic categorization)
scikit-learn>=1.3.0
gensim>=4.3.0  # Alternative topic modeling

# Sentiment analysis
textblob>=0.17.0

# Optional: For advanced URL parsing
urllib3>=2.0.0

# Development dependencies
pytest>=7.3.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0

# Documentation
markdown>=3.4.0

# Installation instructions:
# =========================
# Basic install:
#   pip install -r requirements.txt
#
# For expert features (recommended):
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
#
# Minimal install (URL analysis only):
#   pip install pyyaml requests beautifulsoup4
